[tool.poetry]
name = "shiny-multiprocessing"
version = "0.1.3"
description = ""
authors = ["Maximilian Töpfer <maxtoepfer@mailbox.org>"]
readme = "README.md"
packages = [{include = "shiny_multiprocessing"}]
license = "WTFPL"

[tool.poetry.dependencies]
python = "^3.8"
pebble = "^5.0.3"

[project.urls]
homepage = "https://gitlab.com/SummaSineLaude/shiny_multiprocessing"

[tool.poetry.group.dev.dependencies]
mypy = "^1.1.1"
flake8 = "^6.0.0"
bandit = "^1.7.5"
pytest = "^7.2.2"
pytest-cov = "^4.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
