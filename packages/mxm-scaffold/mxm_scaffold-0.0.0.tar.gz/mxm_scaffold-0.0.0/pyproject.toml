[tool.poetry]
name = "mxm-scaffold"
version = "0.0.0"
readme = "README.md"
description = ""
authors = []
packages = [{include = "mxm_scaffold"}]

[tool.poetry.dependencies]
python = "^3.4"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
