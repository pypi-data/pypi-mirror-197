[build-system]
requires = [
    # cthe minimum version that supports `attr` in setup.cfg
    "setuptools>=46.4.0",
    "wheel"
]
build-backend = "setuptools.build_meta"

[tool.isort]
profile = "black"
combine_as_imports = true
