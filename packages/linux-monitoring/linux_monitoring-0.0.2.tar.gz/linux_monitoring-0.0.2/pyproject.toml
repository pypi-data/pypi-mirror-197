[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "linux_monitoring"
version = "0.0.2"
authors = [
  { name="dheeraj.banodha", email="dheeraj.banodha@impetus.com" },
]
description = "Generates graphical reports for Linux metrics"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]

#[project.urls]
#"Homepage" = ""
#"Bug Tracker" = ""